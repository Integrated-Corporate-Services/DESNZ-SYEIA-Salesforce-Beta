<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_Decision_Issued_CaseOfficer</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    NOT($Permission.Bypass_Validation_Rule),
    NOT(ISNEW()),
    ISPICKVAL(PRIORVALUE(Status__c), &quot;Decision Issued&quot;),
    NOT(ISPICKVAL($User.Persona__c, &quot;Decision Manager&quot;))
)</errorConditionFormula>
    <errorMessage>This record is read-only while the Case Status is ‘Submitted for approval’.</errorMessage>
</ValidationRule>
