<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Edit_When_Archived</fullName>
    <active>true</active>
    <description>When a Case is Archived, then prevent edits on that case.</description>
    <errorConditionFormula>AND(
    ISCHANGED(Status__c) = FALSE,
    ISPICKVAL(Status__c, &quot;Archived&quot;),
				NOT($Profile.Name = &quot;System Administrator&quot;)
)</errorConditionFormula>
    <errorMessage>This case has been marked as Archived and can no longer be edited.</errorMessage>
</ValidationRule>
